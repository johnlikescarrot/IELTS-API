# Markdown lint rules used by super-linter and by editors.
# Line length is not enforced: tables and long URLs are intentional.
MD013: false
# The first heading in each document is the document title.
MD041: false
# Fenced code blocks do not need a language when they contain data.
MD040: false
MD033: false
MD024: false
# JOSS papers carry their title in the YAML front matter and use a top-level
# heading for each section (Summary, Statement of need, References, ...), so
# `paper/paper.md` legitimately has several H1s.
MD025: false
